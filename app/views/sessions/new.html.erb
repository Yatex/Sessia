<section class="page-header">
  <div>
    <p class="eyebrow"><%= t("sessions.index.eyebrow") %></p>
    <h1><%= t("sessions.new.title") %></h1>
  </div>
</section>

<section class="form-panel">
  <%= render "form", session_record: @session %>
</section>
