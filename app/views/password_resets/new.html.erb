<main class="auth-page">
  <section class="auth-panel">
    <div class="auth-heading">
      <%= link_to "Sessia", root_path, class: "public-brand" %>
      <h1>Reset password</h1>
      <p>We will email a secure reset link.</p>
    </div>

    <%= form_with url: password_resets_path, class: "form-stack" do |form| %>
      <div class="field">
        <%= form.label :email %>
        <%= form.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>
      <%= form.submit "Send reset link", class: "primary-button full" %>
    <% end %>

    <p class="auth-switch"><%= link_to "Back to sign in", sign_in_path %></p>
  </section>
</main>
